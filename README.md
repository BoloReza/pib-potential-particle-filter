# Potential GDP with Particle Filter (Ecuador)

This repository contains:
- `Serie.xlsx`: annual real GDP series.
- `calculo_pib_potencial_pf.py`: Python script to estimate potential GDP using a particle filter (local level model).
- `Methodological_Note_Particle_Filter_English.docx`: methodological note (sections 1.1–1.6).

## Requirements

- Python 3.10+ (or similar)
- Packages:
  - `numpy`
  - `pandas`
  - `openpyxl` (for Excel)

You can install them with:

```bash
pip install numpy pandas openpyxl
